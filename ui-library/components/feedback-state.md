# FeedbackState

统一反馈状态族，用于接口动作和任务执行回执。

## Ant Design 对应

`Message` + `Notification` + `Result` + `Alert`。

## 选型建议

- 轻操作即时反馈：`Message`
- 需要上下文和动作入口：`Notification`
- 页面级成功/失败：`Result`
- 区块级提示：`Alert`
