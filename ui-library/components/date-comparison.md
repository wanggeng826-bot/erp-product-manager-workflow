# DateComparisonControl

报表页面时间范围与对比策略的组合控件。

## Ant Design 对应

`RangePicker` + `Segmented` / `Radio.Group` + `Tooltip`。

## 标准选项

- 环比上周期
- 同比去年同期
- 不对比

## 规则

- 时间范围和对比方式应同时进入查询参数。
- 切换对比方式不应清空时间范围。
