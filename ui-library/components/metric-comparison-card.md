# MetricComparisonCard

报表 KPI 指标卡组件，强调“当前值 + 趋势对比”。

## Ant Design 对应

`Card` + `Statistic` + `Tooltip` / `Popover`。

## 展示规范

- 指标名称
- 当前值（主视觉）
- 趋势（环比/同比）
- 样本说明

## 反模式

- 指标卡堆满首屏但无可操作路径。
- 颜色只做装饰，不承载语义。
