# EditDrawer

`DetailDrawer` 的编辑配对组件，用于中等复杂度表单修改。

## Ant Design 对应

`Drawer` + `Form` + `Input` + `Select` + 底部固定操作区。

## 适用场景

- 字段数量在 6~20 之间。
- 需要保留列表上下文，不希望页面跳转。
- 保存前需要统一校验。

## 状态覆盖

- 默认：展示可编辑字段。
- 加载：字段骨架 + 保存按钮 loading。
- 错误：字段级报错 + 顶部错误摘要。
- 禁用/无权限：字段只读且给出原因。
